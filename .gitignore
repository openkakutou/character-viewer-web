node_modules/
dist/
*.local
.DS_Store

# Downloaded/locally-built WASM artifacts from the character library
public/wasm/

# Visual-regression test artifacts (playwright.config.ts's `outputDir`
# failure output) — the committed baselines themselves live under
# tests/visual/**/*-snapshots/ and are tracked normally.
test-results/
playwright-report/
blob-report/
