# Data models

No project-specific data models yet — the codebase currently only has an app entry point and a version constant. This file will grow once character data (sprites, animations, palettes, states) is modeled on the TypeScript side of the WASM bridge.
