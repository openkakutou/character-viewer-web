# Module: game-mode
**Role:** The in-game preview mode (item 008): a screen hosted as a fifth section of `modules/shell.md`'s workspace, listing every one of the loaded character's animations as its own trigger button rather than `viewer`'s Animation section's dropdown/step/manual-loop/collision-overlay debug UI. Clicking a button plays that animation immediately, looping continuously by default (no opt-in checkbox, unlike the debug player) so it reads as "how this behaves in a match" rather than a frame inspector — see `.vibe/decisions/012-in-game-preview-trigger-buttons-loop-and-stop-toggle.md`. Clicking a different animation's button while one is playing replaces it cleanly (its own timer chain and any in-flight decode are cancelled first, same selection-token-guarded pattern as the sprite browser/animation player); clicking the *currently playing* animation's own button again stops it in place instead of restarting it or being a no-op — the only "stop" affordance this section has, since a separate pause control would duplicate the debug Animation tab it deliberately avoids being. Reuses `viewer/animation-player.ts`'s already-exported pure timing helpers (`effectiveTickDuration`, `isBlankFrame`, `computeNextFrameIndex`) and `viewer/sprite-browser.ts`'s decode/draw helpers (`computeScaleToFit`, `defaultDrawPixels`) rather than embedding the whole debug player component. Returns the same small `{ pause() }` handle shape as the animation player so `shell`'s workspace can auto-pause it (and clear its active button's pressed state) when the user navigates away from its section, per item 020's existing convention.

The Special Moves list (item 009) is a sixth, near-twin section: the same trigger-button-list-plus-shared-stage shape, keyed by Statedef (State) instead of by raw animation number. `special-move-list.ts`'s own pure `resolveStateAnimation(stateDef, animations)` resolves each state's `anim` header field to the `Animation` it plays — unset (`0`) defaults to the state's own `number` per MUGEN/Ikemen convention; an unevaluated trigger expression (`stateDef.headerExprs.anim` set) or a number matching no loaded animation both resolve to `null`, meaning "no clearly associated animation" (out of scope to evaluate the expression itself — see the backlog item's own Description). A `null` resolution keeps its list button clickable (with a muted inline hint, never `disabled`) and, once clicked, shows a distinct non-error status with the decode pipeline skipped entirely, rather than reusing the blank-frame/error status paths. It is its own section with its own preview stage rather than a second list feeding `animation-triggers.ts`'s own state machine — see `.vibe/decisions/013-special-moves-own-section-not-merged-into-in-game-preview.md`.
**Files:** `src/game-mode/animation-triggers.ts`, `src/game-mode/special-move-list.ts`
**Exports:** `renderAnimationTriggers(root: HTMLElement, character: CharacterData | null, sffBytes: Uint8Array | null, options?: AnimationTriggersOptions): AnimationTriggersHandle`, `AnimationTriggersHandle` (`pause`), `AnimationTriggersOptions`, `renderSpecialMoveList(root: HTMLElement, character: CharacterData | null, sffBytes: Uint8Array | null, options?: SpecialMoveListOptions): SpecialMoveListHandle`, `SpecialMoveListHandle` (`pause`), `SpecialMoveListOptions`, `resolveStateAnimation(stateDef: StateDef, animations: readonly Animation[]): Animation | null`
**Depends on:** `modules/wasm.md` (`CharacterData`, `resolveSpritePixels`, `SpritePixelResult`, `WasmBridgeOptions`, `Frame`, `Animation`, `StateDef`), `modules/viewer.md` (`effectiveTickDuration`, `isBlankFrame`, `computeNextFrameIndex`, `MS_PER_TICK` from `animation-player.ts`; `computeScaleToFit`, `defaultDrawPixels` from `sprite-browser.ts`)
