> Generated by /vibe:docs — manual edits will be overwritten; use README for hand-written content.

# Testing

## Running the suite

```sh
npm test        # single run
npm run test:watch   # watch mode
```

Vitest, `jsdom` environment. Test files are co-located with the source they
test (`*.test.ts` next to the file it covers).

## Fixture-driven tests against the real WASM module

Tests that touch character parsing (`src/wasm/bridge.test.ts`,
`src/input/character-file-input.test.ts`, `src/input/character-file-input-view.test.ts`,
and `src/main.test.ts`'s end-to-end describe block, which drives the real
file picker through to the rendered characteristics panel) run against
the **real** `character.wasm` module, not a mock — they read
`public/wasm/character.wasm` + `wasm_exec.js` straight off disk via
`node:fs` and inject them into the bridge's `fetchWasmExecSource`/
`fetchWasmBytes` options, so no dev server or real network access is
needed. Run `npm run wasm:download -- <version>` first if `public/wasm/`
is empty (see `docs/development.md`).

Minimal `.air`/`.sff`/`.cns` fixtures live in `src/wasm/testdata/`, copied
from the `character` repo's own test data rather than depending on a
sibling checkout at test time. Expected values in these tests are pinned
from running the real module once against the fixtures — never derived
from the same mapping code the test is meant to catch bugs in.

## jsdom gaps worked around in tests

The project's pinned `jsdom` version does not implement `Blob#arrayBuffer()`
at all. `src/input/character-file-input.ts` reads `File` objects via
`FileReader#readAsArrayBuffer` instead, which behaves identically under
jsdom and in a real browser — confirmed against a real headless Chromium
during this feature's development, not just re-trusted from the same jsdom
that hid the original gap. Async DOM updates driven by that `FileReader`
read (and by the real WASM call) resolve on a real timer tick, not a
microtask — tests wait for them with `vi.waitFor(...)` rather than a fixed
number of chained `Promise.resolve()`s.

Native browser objects that jsdom doesn't construct in a test-friendly way
(`input.files`, a drop event's `dataTransfer`) are stubbed with
`Object.defineProperty` in tests rather than built through jsdom's
(partial) `DataTransfer`, which isn't implemented at all in this jsdom
version.

## Testable-by-construction patterns

Every layer that touches the outside world (network `fetch`, WASM
instantiation, file reads) takes its effect as an injectable
option — `WasmBridgeOptions.fetchWasmExecSource`/`fetchWasmBytes`,
`CharacterFileInputOptions.readFileBytes` — defaulting to the real
implementation but overridable in tests. This is the same shape used by
`scripts/download-wasm.mjs`'s testable `main(argv, overrides)`.

## Beyond the test suite: real-browser verification

Passing tests are not treated as proof a UI feature works. Features that
render DOM are additionally driven against a real headless Chromium
(dev server + Playwright) during development — exercising drag-and-drop,
keyboard-only operation, and visual states a jsdom-only test can't fully
confirm. This isn't part of `npm test`; it's a manual verification step
recorded in the relevant backlog item/decision, not a CI gate.
